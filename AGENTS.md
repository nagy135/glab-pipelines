# Repository instructions

- At the end of every turn that changes code, build the project with `make build` so `bin/glab-pipelines` contains the latest changes before handing the work back to the user.
- Run relevant tests before the final build.
